---
import PageLayout from "~/layouts/PageLayout.astro";
import ContentTag from "~/components/ContentTag.astro";
import SubpageHeader from "~/components/SubpageHeader.astro";
import { getCollection } from "astro:content";
import { formatDate } from "~/utils";

const articles = (await getCollection("articles")).sort(
  (a, b) => b.data.published.valueOf() - a.data.published.valueOf()
);

---

<PageLayout title="Text">
  <main class="the-center">
    <SubpageHeader title="Text" description="Writing notes and longer essays." />

    <ul>
      {articles.map((page) => (
        <li>
          <a href={`/articles/${page.id}/`}>
            <h3>{page.data.title}</h3>
            {page.data.description && (
              <p>{page.data.description}</p>
            )}
          </a>
          <div class="meta">
            <div class="published-date">{formatDate(page.data.published)}</div>
            {page.data.tags.length > 0 && (
              <div class="tags">
                {page.data.tags.map((tag) => <ContentTag tag={tag} />)}
              </div>
            )}
          </div>
        </li>
      ))}
    </ul>
  </main>
</PageLayout>


<style>
main {
  padding-inline: var(--s0);
}

h3 {
  margin: 0;
}
p {
  margin: 0;
}
a p {
  margin-block-start: var(--s0);
}

ul {
  list-style: none;
  padding: 0;
  margin: 0;
  inline-size: 100%;
  max-inline-size: var(--measure);
}

a {
  display: block;
  padding-block-start: var(--s1);
  padding-block-end: var(--s-1);
  border-top: 1px solid var(--color-border);
  text-decoration: none;
  color: inherit;
}

a:hover,
a:focus-visible {
  color: var(--color-accent);
}

.meta {
  display: flex;
  align-items: baseline;
  gap: var(--s-2);
  padding-block-end: var(--s1);
}

.published-date {
  color: var(--color-ink-muted);
}

.tags {
  margin-inline-start: auto;
  display: flex;
  flex-wrap: wrap;
  gap: var(--s-5);
}
</style>