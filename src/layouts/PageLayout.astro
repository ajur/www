---
import { Font } from "astro:assets";

import BaseLayout from "./BaseLayout.astro";

import "~/styles/page.css";
import "~/styles/callouts.css";

import Footer from "~/components/Footer.astro";
import TopNavBar from "~/components/TopNavBar.astro";
import MainMenu from "~/components/MainMenu.astro";
import Logo from "~/components/Logo.astro";

const {
  title = "Astro",
  bkgMode = "quiet",
  bkgQuietTarget = "article",
} = Astro.props;
---

<BaseLayout title={title} bkgMode={bkgMode} bkgQuietTarget={bkgQuietTarget}>
  <Font cssVariable="--font-baskervville" />
  <Font cssVariable="--font-smooch-sans" />
  <TopNavBar />
  <slot />
  <Footer class="footer-main-menu">
    <MainMenu aria-label="Footer menu" class="bg-drop-shadow" />
  </Footer>
  <Footer class="copyright">
    <div class="bg-drop-shadow">
      &copy; {new Date().getFullYear()} Adam Jurczyk.
      <br />
      Site content and artwork licensed under 
      <a href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank">CC BY-NC 4.0</a>.
    </div>
    <Logo />
  </Footer>
</BaseLayout>
<style>
  .footer-main-menu {
    font-size: var(--s-1);
    display: flex;
    justify-content: center;
  }
  .copyright {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--s0);
    font-size: var(--s-1);
    color: var(--color-ink-muted);
  }
  .copyright a {
    color: inherit;
  }
</style>